# Werk van Sil

# for-loop
# [] 


def validate_appointment(data: dict):
    KeyList = ['title', 'description', ...]
    for key in KeyList:
        if key not in dict:
            return False
        else:
            continue
